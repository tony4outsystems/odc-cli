[tasks.demo]
description = "Regenerate demo.gif using VHS"
run = """
set -e
echo "Building odc in release mode..."
cargo build --release
echo ""
echo "Recording demo with VHS..."
export PATH="$PWD/target/release:$PATH"
vhs scripts/demo/demo.tape
"""
