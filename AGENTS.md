# Agent notes

- Do not use the `outsystems` MCP server for tasks in this repo. Use the OutSystems ODC CLI (`odc`)
  (Rust, `src/cli.rs` and `src/commands.rs`) instead, adding commands to it as needed.
